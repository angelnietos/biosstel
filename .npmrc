# pnpm is the package manager for this repo (see package.json "packageManager" and "pnpm" config).
# pnpm-specific options live in package.json "pnpm" to avoid npm warnings when using npx.
# This file is kept for any future options that both npm and pnpm understand.
