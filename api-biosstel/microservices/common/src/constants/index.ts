import DATABASES from "./databases_config";
import ENV from "./env";
import ERROR_CODES from "./error_codes";
import STRING_CODES from "./string_codes";
import STRINGS from "./strings";
import DEVELOPMENT_KEYS from "./development_keys";

const SHARED_CONSTANTS = {
  DATABASES,
  ENV,
  ERROR_CODES,
  STRING_CODES,
  STRINGS,
  DEVELOPMENT_KEYS,
};

export default SHARED_CONSTANTS;
