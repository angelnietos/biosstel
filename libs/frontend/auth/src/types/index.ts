/**
 * @biosstel/auth - Types
 * 
 * Re-exports shared types and defines frontend-specific types if needed.
 */

// Re-export all shared types
export type {
  User,
  AuthUser,
  Role,
  Permission,
  Organization,
  LoginCredentials,
  RegisterData,
  AuthResponse,
} from '@biosstel/shared-types';

// Frontend-specific types (if any) would go here
// For now, all types are shared
