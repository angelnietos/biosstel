/**
 * Shell de productos: wrapper para páginas de la feature.
 * La app puede usar ProductosPage directamente; este barrel mantiene la estructura estándar.
 */
export { ProductosPage } from '../pages';
