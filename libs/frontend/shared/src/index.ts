/**
 * @biosstel/shared - Shared UI Components Library
 * 
 * Components shared across multiple features.
 * These are NOT atomic (those are in @biosstel/ui) but are
 * reusable compositions used by features.
 */

// Layout Components
export { AuthLayout, type AuthLayoutProps } from './components/AuthLayout';
export { PageContent, type PageContentProps } from './components/PageContent';

// Form Components
export { Button, type ButtonProps } from './components/Button';
